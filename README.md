# pfscracker
A folder scanner for PFS Professional Write 1988 documents that recursively walks a directory, identifies every file as encrypted-PFS, plain-PFS, or non-PFS, and recovers the password from each encrypted document instantly by reversing the slot cipher.
